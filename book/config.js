// ---------------------------------------------------------------------------
// Fill these two in from your Supabase project:
//   Supabase dashboard → Project Settings → Data API
// The anon key is designed to be public — every table is protected by the row
// level security policies in supabase/schema.sql, so shipping it in the browser
// is how Supabase is meant to be used.
// ---------------------------------------------------------------------------
export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-PUBLISHABLE-ANON-KEY';

// Where the magic-link email should send people back to.
export const REDIRECT_URL = 'https://thesocialclubconsultant.com/book/';

export const CLUB_NAME = 'Three Cities Social';
